spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/memall
    username: root
    password: root

  redis:
    database: 0
    host: localhost
    port: 6379

server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml

